import React from 'react';
import ReactDOM from 'react-dom';

const Markers = (props) => {
  return (
    <div>
      <h1>{bar.name}</h1>
      <p>{bar.formatted_address}</p>
      <p>Rating: {bar.rating}</p>

      <button onClick={this.handleWindowClick} >Add bar to crawl</button>
    </div>
  )
}

export default Markers;